language: go
go: 1.8
jobs:
  allow_failures:
    - go: master
  fast_finish: true
install:
    - # skip
script:
    - go get -t -v ./...
    - diff -u <(echo -n) <(gofmt -d -s .)
    - go tool vet .
    - go test -v -race ./...